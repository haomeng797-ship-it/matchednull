# matchednull

Matched-null tests for cluster-count claims: does a reported number of "types" exceed what the data's own margins and covariance already produce?

Companion package to the paper *Types Without Taxa*. In development; CRAN submission planned.
